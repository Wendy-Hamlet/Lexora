# Runtime dependencies — kept in sync with pyproject.toml [project.dependencies].
# Install with: pip install -e .
# Or pinned: pip install -r requirements.txt
pydantic>=2.6
pyyaml>=6.0
httpx>=0.27
typer>=0.12
rich>=13.7
beautifulsoup4>=4.12
lxml>=5.0
pymupdf>=1.24
rapidfuzz>=3.6
rank-bm25>=0.2.2
numpy>=1.26

# Optional extras (install via pip install -e ".[browser]" / ".[api]" / ".[embeddings]" ...):
#   browser    : playwright>=1.44   (headless-browser fetch for SG SSO / AU SPA; then `playwright install chromium`)
#   api        : fastapi, uvicorn   (audit UI)
#   embeddings : sentence-transformers, torch (planned hybrid retrieval)
#   ocr        : pytesseract, Pillow
#   llm        : openai
